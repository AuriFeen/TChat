# 💬 TChat

**A minimalist, cross-platform terminal chat application built in C and C++.**

---

## 🚀 Project Overview

**TChat** is a high-performance, command-line interface (CLI) chat application designed for users who appreciate speed, efficiency, and a text-only environment. Built from the ground up using **pure C and C++**, TChat runs natively on **Windows, macOS, and Linux**, ensuring a seamless experience regardless of your operating system.

It's designed to run directly in your terminal (e.g., Command Prompt, Terminal, or Bash), keeping dependencies minimal and system resource usage low.

## ✨ Features

* **💻 Cross-Platform Compatibility:** Runs flawlessly on **Windows, macOS, and Linux**.
* **💨 Minimalist & Fast:** Built in C/C++ for native speed and a clean, distraction-free terminal interface.
* **🔒 Secure Connection:** (Optional: **Note:** Replace this with your actual network implementation. If it's local only, update this line.) Utilizes a robust networking protocol for secure data transfer.
* **🔧 Minimal Dependencies:** No external runtimes or libraries needed beyond standard C/C++ build tools.
* **📜 Command-Line Driven:** All features are accessible via simple terminal commands.

## ⚙️ Installation

To get TChat up and running, you'll need a C/C++ compiler (like GCC, Clang, or MSVC) and `make` (or equivalent build system) installed on your system.

### 1. Clone the Repository

```bash
git clone [https://github.com/AuriFeen/TChat.git](https://github.com/AuriFeen/TChat.git)
cd TChat
